---
description: 功能代码变更后自动同步更新 README.md
alwaysApply: true
---

# README 同步维护

完成 **live-panel** 项目中任何功能性代码变更（新模块、新脚本、新路由、UI 行为变更、API、构建/部署流程）后，**在同一任务内**更新 `README.md`，无需用户再次提醒。

## 必须同步的章节

视变更范围更新对应部分，至少检查：

1. **项目概览 / 访问地址** — 新入口、URL、端口
2. **快速开始 / npm 脚本** — 新命令或流程变化
3. **特性 / 核心功能** — 用户可见能力
4. **项目结构** — 新增或移动的文件/目录
5. **更新日志** — 在「版本与更新日志」追加条目（版本号 + 日期 + 要点）
6. **故障排除** — 新常见问题

## 更新日志格式

```markdown
### [x.y.z] - YYYY-MM-DD

- 简述变更（为什么 / 用户影响）
```

版本号遵循：新功能 minor+1，修复 patch+1，大改 major+1；与 `package.json` version 保持一致。

## 不做的事

- 纯样式微调且不影响使用方式时，仅更新日志一行即可，不必重写整篇
- 不要删除 README 中仍有效的历史说明
- 不要为用户未要求的其他 markdown 文件主动大改（`CHANGELOG.md` 可在发布时同步一条）

## 发布时

若用户要求发布/部署：先 `npm run build`，确认 `dist/` 含 `index.html`、`teachzhao/` 等，再 commit；仅当用户明确要求时才 push。
